import React from "react";
import Container from "../shared/Container";

const Attendies = ({open, setOpen}) => {
  return (
    <Container open={open} setOpen={setOpen}>
      <div className="chart-progress">
        
      </div>
    </Container>
  );
};

export default Attendies;
